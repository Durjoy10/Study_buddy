{
  "type": "module",
  "scripts": {
    "build": "ng build",
    "vercel-build": "npm install -g @angular/cli && ng build --configuration production"
  },
  "devDependencies": {
    "@angular/cli": "^17.0.0",
    "@angular-devkit/build-angular": "^17.0.0"
  }
}